type Query {
    user(id: ID!): User
    users: [User]
}

type User {
    id: ID!
    name: String
    email: String
    password: String
}

type Mutation {
    createUser(input: CreateUserInput!): User
}

input CreateUserInput {
    name: String!
    email: String!
    password: String!
    address: String
}